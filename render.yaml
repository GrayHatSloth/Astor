services:
  - type: web
    name: astor-bot
    env: python
    branch: main
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    disk: 10
    autoDeploy: true
